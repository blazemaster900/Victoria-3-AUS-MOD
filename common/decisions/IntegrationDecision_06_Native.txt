Native_Decision = {
	is_shown = {
		has_variable = OpenIntegrationDecision_variable
		NOT = { has_global_variable = native_merged }
		NOT = { has_global_variable = all_merged }
		NOT = { has_global_variable = hide_decision }
	}

	possible = {
		always = yes
	}

	when_taken = {
		trigger_event = { id = Integration_events.1 }	
		set_global_variable = native_merged
	}
}

